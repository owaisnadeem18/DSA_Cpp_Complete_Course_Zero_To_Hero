#include <iostream>
using namespace std;

int main()
{
    int num = 10;
    cout << "Value of num: " << num << endl;
    cout << "Memory address of num: " << &num << endl; // &num gives the memory address of the variable num (This is all about memory address in cpp)
    return 0;
}

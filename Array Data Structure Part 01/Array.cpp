#include <iostream>
using namespace std;

int main()
{
    // Declaring an array of integers with 5 elements
    // int numbers[5];

    // // Assigning values to the array
    // numbers[0] = 10; // First element
    // numbers[1] = 20; // Second element
    // numbers[2] = 30; // Third element
    // numbers[3] = 40; // Fourth element
    // numbers[4] = 50; // Fifth element

    // // Accessing array elements by index and printing them
    // cout << "Value at index 0: " << numbers[0] << endl;
    // cout << "Value at index 1: " << numbers[1] << endl;
    // cout << "Value at index 2: " << numbers[2] << endl;
    // cout << "Value at index 3: " << numbers[3] << endl;
    // cout << "Value at index 4: " << numbers[4] << endl;

    int arr[5] = {0, 1, 2, 3, 5};

    cout << "The length of arr is " << sizeof(arr) << endl;

    return 0;
}
